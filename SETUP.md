# 재고/CS환불/순이익 자동 분석 시트 — 설정 가이드

## 무엇을 만들었나
구글시트 하나에 Gmail·카카오톡(거래처 엑셀 업로드)·발주플랫폼(greenmoment CSV/엑셀 다운로드) 세 채널의 주문을
자동으로 모아 표준 형식으로 정리하고, 품목마스터 정보와 결합해 원물재고·박스재고·CS환불비용·품목별/옵션별
순이익을 실시간(수식 기반) + 주기적 자동수집(Apps Script 트리거)으로 계산합니다.

코드는 `apps-script/` 폴더에 있습니다. 아래 순서대로 진행하세요.

## 1. 구글시트 생성 + 스크립트 붙여넣기
1. drive.google.com에서 새 구글시트를 만듭니다 (예: "재고분석").
2. 상단 메뉴 **확장 프로그램 > Apps Script** 클릭.
3. 왼쪽 톱니바퀴(프로젝트 설정)에서 **"appsscript.json" 매니페스트 파일을 편집기에서 보기**를 체크합니다.
4. 좌측 파일 목록의 `appsscript.json`을 열고, 이 폴더의 [appsscript.json](apps-script/appsscript.json) 내용으로 전체 교체합니다.
5. `+` 버튼으로 스크립트 파일을 새로 만들고, 아래 순서로 `apps-script/` 폴더의 각 `.gs` 파일 내용을 **파일명 그대로** 복사해 넣습니다.
   (Apps Script는 파일 실행 순서를 신경 쓰지 않지만, 파일명은 참고용으로 동일하게 맞춰두면 관리가 편합니다.)
   - Constants.gs
   - ConfigStore.gs
   - TabularFileReader.gs
   - Normalizer.gs
   - SheetBootstrap.gs
   - GmailCollector.gs
   - DriveUploadCollector.gs
   - Menu.gs
   - Triggers.gs
   - CourierFormConverter.gs
   - LaborManager.gs
   - PlatformUploadConverter.gs
6. 기본 생성되는 `Code.gs`는 지워도 됩니다.

## 2. 고급 서비스(Drive API) 켜기
Gmail 첨부로 오는 xlsx 파일을 변환하려면 Advanced Drive Service가 필요합니다.
1. Apps Script 편집기 왼쪽 **서비스** 옆 `+` 클릭.
2. **Drive API**를 선택하고 식별자(Identifier)가 `Drive`인지 확인 후 추가.
   (이미 appsscript.json에 명시해두었으므로, 처음 저장 시 자동으로 잡히는 경우도 있습니다.)

## 3. 초기 시트 생성
1. 구글시트로 돌아와 새로고침(F5)하면 상단에 **"재고분석"** 메뉴가 생깁니다.
2. **재고분석 > 초기 시트 생성** 클릭 → 처음 실행 시 구글 계정 권한 승인 팝업이 뜨면 승인합니다.
   (Gmail 읽기, Drive 접근, 스프레드시트 편집 권한을 요청합니다 — 이 시트/계정 안에서만 사용됩니다.)
3. 완료되면 아래 탭들이 자동 생성됩니다.

| 시트 | 용도 | 입력 방식 |
|---|---|---|
| 설정 | Gmail 검색어, 드라이브 폴더 ID 등 | 수동 |
| 컬럼매핑 | 각 채널 원본 파일의 컬럼명 → 표준 필드명 매핑 | 수동 |
| 품목마스터 | 품목/옵션별 원가·판매가·원물/박스 환산계수 | 수동 |
| CS환불규정 | 환불사유 코드/유형 정의 | 수동 |
| 원물입고내역 / 박스입고내역 | 실제 매입 입고 기록 | 수동 |
| 원물기초재고 / 박스기초재고 | 시스템 도입 시점의 기초 재고량 | 수동(1회) |
| 주문_Gmail / 주문_카카오톡 / 주문_발주플랫폼 | 채널별 원본 주문 (자동 수집) | 자동 |
| 통합주문 | 세 채널을 합친 뷰 | 수식(자동) |
| CS환불내역 | 환불 건 기록 | 수동 |
| 주문상세계산 | 주문별 원물/박스 사용량·매출·원가 계산 | 수식(자동) |
| 재고_원물현황 / 재고_박스현황 | 실시간 재고 계산 결과 | 수식(자동) |
| 순이익_품목별옵션별 | 품목·옵션별 매출/원가/CS환불/순이익 | 수식(자동) |

## 4. 설정 시트 채우기
- `GMAIL_SEARCH_QUERY`: 발주 메일을 걸러내는 Gmail 검색어. 거래처 발주 메일에 Gmail 라벨(예: `발주`)을
  걸어두고 `label:발주 has:attachment newer_than:14d` 형태로 두는 것을 권장합니다.
- `KAKAO_FOLDER_ID` / `PLATFORM_FOLDER_ID`: 거래처 엑셀, greenmoment 다운로드 파일을 넣을 드라이브 폴더를
  만들고 그 폴더 ID(URL의 `/folders/` 뒤 문자열)를 넣습니다.
- `KAKAO_DONE_FOLDER_ID` / `PLATFORM_DONE_FOLDER_ID`: 처리 완료된 파일을 옮길 별도 폴더(선택, 비워두면 원본 폴더에 그대로 둠).
- `REFRESH_INTERVAL_MINUTES`: 자동 수집 주기(분). 1/5/10/15/30 중 가장 가까운 값으로 적용됩니다.

카카오톡 발주서는 거래처에서 받은 엑셀 파일을 사람이 그대로 `KAKAO_FOLDER_ID` 폴더에 업로드하면 되고,
발주플랫폼은 관리자 화면에서 CSV(가능하면 CSV 우선, 엑셀이면 아래 5번 설정 필요)를 받아 `PLATFORM_FOLDER_ID`
폴더에 업로드하면 됩니다.

## 5. 드라이브 업로드 파일이 엑셀(.xlsx)인 경우
`readTabularFile_`는 CSV 또는 "Google 스프레드시트" 형식만 바로 읽습니다. 드라이브에 xlsx를 업로드해도
자동으로 구글시트로 변환되게 하려면:
- 드라이브 설정(톱니바퀴 > 설정)에서 **"업로드한 파일을 Google 문서 편집기 형식으로 변환"**을 켜두세요.
- 이 옵션이 없거나 켜지지 않는 환경이면, 되도록 CSV로 다운로드/저장해서 올려주세요.

## 6. 컬럼매핑 시트 채우기 (가장 중요)
실제 파일의 헤더 텍스트를 표준 필드로 연결하는 표입니다. 실제로 주신 샘플 3개(카카오톡파일.xlsx, 메일발주.xlsx,
관리자페이지 파일.xlsx)를 확인하고, 사장님이 확인해주신 내용까지 반영해서 초기 시트 생성 시 아래 내용이
자동으로 채워집니다 (직접 입력하실 필요 없음).

| 소스 | 원본컬럼명 | 표준필드명 |
|---|---|---|
| 카카오톡 | 주문번호 | 주문번호 |
| 카카오톡 | 상품명1 | 품목명 |
| 카카오톡 | 수량(소) | 수량 |
| 카카오톡 | 받는사람 | 비고 |
| 카카오톡 | 보내는사람(지정) | 비고 |
| Gmail | 고객주문번호 | 주문번호 |
| Gmail | 품목명 | 품목명 |
| Gmail | 박스수량 | 수량 |
| Gmail | 집하예정일 | 주문일시 |
| Gmail | 받는분성명 | 비고 |
| Gmail | 보내는분성명 | 비고 |
| 발주플랫폼 | 상품주문번호 | 주문번호 |
| 발주플랫폼 | 상품명 | 품목명 |
| 발주플랫폼 | 수량 | 수량 |
| 발주플랫폼 | 수령인 | 비고 |
| 발주플랫폼 | 주문자 | 비고 |

표준필드명은 다음 중에서만 사용합니다:
`수집일시, 소스, 원본식별자, 주문일시, 주문번호, 거래처명, 품목명, 옵션명, 수량, 단위, 판매단가, 공급단가, 비고`
(수집일시/소스/원본식별자는 자동 채워지므로 매핑하지 않아도 됩니다.)

**세 파일과 품목/가격 파일(`상품공급가및매입가.xlsx`)을 확인해서 정리된 사실:**
- 이 사업은 **여러 농장(매입처)에서 상품을 사입해서 여러 판매채널(매출처)에 공급하고, 최종 고객에게는
  직배송하는 구조**입니다. 그래서 주문 파일 3개는 전부 "최종 고객한테 뭘 몇 개 보내라"는 배송 지시서일 뿐,
  가격도 없고 "어느 거래처(매입처) 상품인지"도 적혀있지 않습니다.
- 카카오톡의 "보내는사람(지정)", Gmail의 "보내는분성명", 발주플랫폼의 "주문자"는 실제 매입 거래처와
  다르다고 확인되어 `거래처명`으로 매핑하지 않고 `비고`에만 참고용으로 남겨뒀습니다. **거래처(매입처)는
  품목마스터의 `매입처명` 칸에서 품목 단위로 관리합니다.**
- 매출액·원가는 전부 `품목마스터`에서 품목명으로 찾아옵니다 — `품목마스터`의 `품목명` 칸에는 주문에 찍히는
  것과 **완전히 똑같은 문자열**을 그대로 입력해야 매칭됩니다. `옵션명`은 비워두세요(주문 쪽에서도 옵션명을
  따로 매핑하지 않았습니다). 7번에서 이어서 설명합니다.
- 카카오톡파일의 `상품명2`(두 번째 상품 칸)는 실제로 채워지는 일이 없다고 확인되어 매핑하지 않았습니다.
- 컬럼매핑을 고치거나 다른 파일 형식이 추가로 있으면 `컬럼매핑` 시트에서 직접 행을 추가/수정하면 됩니다
  (초기 시트 생성은 이미 값이 있으면 건드리지 않으니 안심하고 수정하세요).

## 7. 품목마스터 / 기초재고 채우기
- `품목마스터`: 품목코드, 품목명, 옵션코드, 옵션명, 박스종류, 매입처명, 판매단가, 매입원가_단위당,
  원물환산계수(옵션 1개 판매 시 소요되는 원물량), 박스환산계수(옵션 1개 판매 시 소요 박스 수), 활성여부.
  **품목명은 주문 파일에 찍히는 문자열과 완전히 동일하게 입력**하세요 (예: `강원도 국내산 팥 1kg`,
  `가정용 수박 3kg`, `한반도 농협 토마토 정품 M_5KG`). `옵션명`은 비워두면 됩니다 — 6번에서 설명했듯이
  세 채널 모두 옵션이 품목명에 이미 포함돼 있어서 옵션명을 따로 매핑하지 않았기 때문입니다.
  - **초기 시트 생성 시 `상품공급가및매입가 (3).xlsx`에 있던 실제 품목 145개를 품목코드/품목명/매입처명/
    매입원가/판매단가까지 전부 미리 채워넣었습니다.** 판매단가(공급가)는 매출처·그룹마다 다르게
    책정돼 있어서(그룹공급가 290줄, 매출처별공급가 183줄), 그 중 매출처 구분이 없는 **"기본공급가"**를
    대표값으로 채택했습니다. 특정 매출처와의 정확한 마진을 보려면 주문 데이터에 "어느 매출처를 통한
    주문인지"가 있어야 하는데 지금 세 채널에는 없어서, 우선 기본공급가 기준으로 순이익을 계산합니다.
    나중에 매출처별로 정확히 나누고 싶으시면 말씀해주세요.
  - 카카오톡 주문 파일의 품목명은 `강원도` 같은 산지 표기가 빠져있는 경우가 있었습니다(예: 주문에는
    "국내산 팥 1kg", 품목마스터엔 "강원도 국내산 팥 1kg"). 이렇게 표기가 다르면 매칭이 안 되니, 실제 주문에
    찍히는 이름 기준으로 품목마스터 품목명을 맞추거나, 둘 중 한쪽 표기를 통일해주세요.
- `원물기초재고` / `박스기초재고`: 시스템을 켜는 시점 기준 실제 재고 수량을 1회 입력.
- `원물입고내역` / `박스입고내역`: 이후 매입/입고가 생길 때마다 한 줄씩 추가.
- `CS환불규정`, `CS환불내역`: 환불 건이 생길 때마다 입력. `원물회수여부`에 "재사용"을 넣으면 해당 수량만큼
  원물/박스 재고가 복구되고, "폐기"로 두면 이미 판매 시 차감된 수량이 그대로 손실 처리됩니다.

## 8. 자동 수집 켜기
**재고분석 > 자동 수집 트리거 설정** 메뉴를 누르면 설정된 주기로 `runAllCollectors`가 자동 실행되어
Gmail/카카오톡 폴더/발주플랫폼 폴더를 모두 수집합니다. 즉시 한 번 실행해보고 싶으면
**재고분석 > 지금 전체 수집 실행**을 누르세요.

## 9. 결과 확인
- `재고_원물현황`, `재고_박스현황`: 품목/박스종류별 실시간 재고.
- `순이익_품목별옵션별`: 품목·옵션별 매출/원가/CS환불비용/순이익/순이익률.
- 실행 로그(에러 등)는 Apps Script 편집기의 **실행** 탭에서 확인할 수 있습니다.

## 10. 택배양식 → 어드민양식 자동 변환기
택배사마다 다른 배송 원본 파일을 매번 손으로 "어드민 양식"에 맞춰 옮기는 작업을 자동화합니다.

1. 드라이브에 폴더 3개를 만듭니다: **택배원본업로드**, **택배원본처리완료**(선택), **어드민양식결과**.
2. 설정 시트에 `COURIER_SOURCE_FOLDER_ID`(택배원본업로드 폴더 ID), `COURIER_DONE_FOLDER_ID`(처리완료 폴더 ID, 선택),
   `COURIER_OUTPUT_FOLDER_ID`(결과 폴더 ID)를 채웁니다.
3. **택배사매핑** 시트에 택배사별 "원본컬럼명 → 어드민양식컬럼명" 매핑을 등록합니다. 초기 시트 생성 시
   `예시택배사`라는 이름으로 한 세트가 미리 채워져 있는데, 이는 `택배양식.xlsx` 샘플 1건을 근거로 추정한
   매핑입니다. **`예시택배사`를 실제 택배사 이름으로 바꾸고, 값이 정확한지 확인해주세요.**
   다른 택배사 파일이 있으면 같은 형식으로 행을 추가하면 됩니다. 어드민양식 컬럼은 다음 12개만 사용합니다:
   `보내는사람, 보내는분전화번호, 받는고객, 받는고객전체주소, 우편번호, 받는고객전화번호, 받는고객핸드폰번호,
   박스수량, 품명01, 배송메세지, 운송장번호, 주문번호`
   설정 시트의 `COURIER_DEFAULT_SOURCE`도 택배사매핑에서 쓴 이름과 똑같이 맞춰주세요(초기값 `예시택배사`).
4. 택배사에서 받은 원본 파일을 `택배원본업로드` 폴더에 올립니다. **택배사가 하나뿐이면 파일명은 아무렇게나
   지어도 됩니다** — `COURIER_DEFAULT_SOURCE`로 지정한 매핑을 자동으로 씁니다. 택배사가 여러 개면 파일명을
   "택배사이름_아무거나.xlsx"로 지어서 구분하세요 (예: `로젠_20260710.xlsx`, `CJ_20260710.xlsx`).
5. **재고분석 > 택배양식 → 어드민양식 변환 실행** 메뉴를 누르면, 각 파일을 매핑에 따라 변환해
   `원본파일명_어드민양식.xlsx` 형태로 `어드민양식결과` 폴더에 저장합니다. 그 파일을 그대로 다운로드해서
   업로드하면 됩니다.
6. xlsx 원본을 바로 읽으려면 2번 섹션의 Drive 업로드-자동변환 설정이 필요합니다(또는 CSV로 올리세요).

## 11. 이모님 인건비 캘린더
일당제로 일하는 근무자의 출근·급여를 관리합니다. 사람마다 고정 일당이 있고, 그날 출근했는지만 체크하면
캘린더가 자동으로 채워지는 방식입니다.

1. **인력마스터** 시트에 근무자를 등록합니다: 이름, 기본일당(예: 100000), 활성여부(반드시 `Y`로 입력해야
   다이얼로그와 캘린더에 나타납니다), 비고.
2. 근무한 날 **재고분석 > 오늘 근무자 추가 (인건비)** 메뉴를 누르면, 활성 근무자 체크박스와 날짜(기본값 오늘)가
   뜹니다. 출근한 사람을 체크하고 "등록"을 누르면 **근무기록** 시트에 그 사람의 기본일당으로 한 줄씩 저장됩니다.
   같은 사람+같은 날짜가 이미 있으면 중복으로 추가되지 않습니다.
3. **근무캘린더** 시트에서 결과를 확인합니다. C1 셀의 "기준월"을 보고 싶은 달의 1일로 바꾸면(예: 2026-08-01),
   그 달의 1일~31일 x 근무자별 일당이 격자로 표시되고, 오른쪽에 그 달 근무일수·합계금액이 자동 계산됩니다.
   (2월처럼 31일이 없는 달은 뒤쪽 날짜 칸이 자동으로 비워집니다.)
4. 잘못 등록한 근무는 `근무기록` 시트에서 해당 행을 직접 지우면 캘린더에도 바로 반영됩니다.
5. 일당이 평소와 다르게 나간 날은 `근무기록`의 `일당` 칸 값을 직접 고치면 됩니다(기본일당과 다르게 낼 수 있음).
6. 지금은 전체 인건비를 품목별 순이익 계산에 자동으로 반영하지 않습니다(인건비는 특정 품목에 딱 떨어지지
   않는 공통비용이라서요). 전체 순이익에서 인건비까지 뺀 숫자를 보고 싶으시면 말씀해주세요 — 대시보드에
   "총매출 - 총원가 - CS환불 - 인건비" 요약을 추가해드릴 수 있습니다.

## 12. 원본파일 → 관리자업로드양식 자동 변환기
카카오톡 등에서 받은 원본 주문 파일을 그린모먼트 관리자페이지에 올리는 업로드 양식으로 바꿔줍니다.
"택배양식 → 어드민양식" 변환기(10번)와 같은 방식이지만 대상 양식이 다릅니다.

1. 드라이브에 폴더 3개를 만듭니다: **업로드변환원본**, **업로드변환처리완료**(선택), **관리자업로드양식결과**.
2. 설정 시트에 `PLATFORM_UPLOAD_SOURCE_FOLDER_ID`, `PLATFORM_UPLOAD_DONE_FOLDER_ID`(선택),
   `PLATFORM_UPLOAD_OUTPUT_FOLDER_ID`를 채웁니다.
3. **업로드양식매핑** 시트에 소스별 컬럼매핑이 있습니다. 초기 시트 생성 시 실제 `카카오톡파일.xlsx` 샘플을
   근거로 `카카오톡` 소스 매핑을 미리 채워뒀습니다 — 원본의 "보내는사람(지정)/전화번호1(지정)/주소(지정)"을
   주문자(발송 주체) 정보로, "받는사람/전화번호1/전화번호2"를 수령인(최종 고객) 정보로 매핑했습니다.
   관리자업로드양식의 `택배사`/`운송장번호`/`상품주문번호`는 원본에 없어서 비워두는데, 이 값들이 업로드 후
   자동으로 채워지는 게 맞는지 확인 부탁드립니다.
4. 원본 파일을 업로드변환원본 폴더에 올립니다. **소스가 카카오톡 하나뿐이면 파일명은 아무렇게나 지어도
   됩니다** — 설정 시트의 `PLATFORM_UPLOAD_DEFAULT_SOURCE`가 기본값 `카카오톡`으로 채워져 있어서, 파일명에
   "소스이름_" 접두사가 없어도 자동으로 그 매핑을 씁니다. 나중에 소스가 여러 개로 늘어나면 그때는 파일명을
   "소스이름_아무거나.xlsx"로 지어서 어느 매핑을 쓸지 구분하면 됩니다.
5. **재고분석 > 원본파일 → 관리자업로드양식 변환 실행** 메뉴를 누르면 `관리자업로드양식결과` 폴더에
   `원본파일명_관리자업로드양식.xlsx`가 생성됩니다. 그 파일을 관리자페이지에 업로드하면 됩니다. 변환이 안 된
   파일이 있으면 화면 아래 알림(토스트)에 어떤 파일이 건너뛰어졌는지 표시됩니다.

## 현재 한계 / 다음 단계
- 컬럼매핑·품목마스터는 실제 파일(카카오톡 발주서, Gmail 주문메일, 관리자페이지 파일, 상품공급가및매입가)을
  근거로 채워뒀지만, 아직 실제 구글시트에 붙여넣고 돌려본 적은 없습니다. 실행하다 나오는 오류는 알려주시면
  바로 고치겠습니다.
- 순이익은 매출처 구분 없는 "기본공급가" 기준입니다. 매출처별 정확한 마진이 필요하면 주문 데이터에
  매출처 정보를 추가로 매핑해야 합니다.
- 택배사매핑의 `예시택배사` 세트는 파일 1건만 보고 추정한 것이라, 실제 택배사 이름과 컬럼이 맞는지 꼭 확인해주세요.
  다른 택배사 원본 파일을 주시면 그 매핑도 정확히 만들어드립니다.
- 근무캘린더는 활성 근무자 최대 30명까지 그리드를 만들어둡니다(`Constants.gs`의 `LABOR_CALENDAR_MAX_WORKERS`).
  30명이 넘으면 이 값을 늘려야 합니다.
- 인건비는 아직 전체 순이익/대시보드에 자동으로 합산되지 않습니다. 필요하시면 추가하겠습니다.
- 대시보드(차트/요약 KPI)는 아직 빈 시트만 만들어둔 상태입니다. 데이터가 쌓이면 시각화를 추가할 수 있습니다.
